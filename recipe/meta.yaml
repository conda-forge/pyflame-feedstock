{% set name = "pyflame" %}
{% set version = "1.6.6" %}
{% set checksum = "03723509c037a994585e81e4a9c6d8e4fed87b486e23edd8d355a7bcc009bf79" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/uber/pyflame/archive/v{{ version }}.tar.gz
  sha256: {{ checksum }}

build:
  number: 0
  skip: true  # [not linux]
  skip: true  # [py>36]

requirements:
  build:
    - autoconf
    - automake
    - libtool
    - pkg-config
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
  host:
    - python

  run:
    - python

test:
  commands:
    - test -f $PREFIX/bin/pyflame

about:
  home: https://pyflame.readthedocs.io/en/latest/
  license: Apache-2.0
  license_file: LICENSE
  license_family: APACHE
  summary: Pyflame is a ptracing profiler for Python.
  dev_url: https://github.com/uber/pyflame

extra:
  recipe-maintainers:
    - eklitzke
    - xhochy
